\chapter{System Architecture Overview}
\label{chap:overview}

\section{Executive Summary}

The HCM City Traffic Camera \& Route Finder is a modern Single Page Application (SPA) designed to solve urban mobility challenges in Ho Chi Minh City. By aggregating multi-modal routing (Car, Bike, Walking) with real-time traffic camera monitoring and itinerary planning, the system offers a unified dashboard for commuters.

The system employs a Decoupled Client-Server Architecture. The visualization layer is a React.js application that remains agnostic of the backend logic, communicating strictly via RESTful API calls. This report details the visual implementation, the logical requirements, and the critical integration points required for deployment.

The website link for this project is: \Large\textbf{\url{https://map.hcmus.fit/}}

\section{Architectural Diagram Description}
The application follows a layered architecture designed for scalability and separation of concerns.


\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    node distance=0.5cm,
    box/.style={rectangle, draw=black, thick, minimum width=12cm, minimum height=1cm, align=left, font=\small},
    bigbox/.style={rectangle, draw=black, very thick, minimum width=12cm, minimum height=4.5cm, align=left},
    arrow/.style={->, >=stealth, thick},
    label/.style={font=\small\itshape}
]

% Presentation Layer
\node[bigbox] (presentation) at (0,0) {
    \textbf{PRESENTATION LAYER (Client)} \\[0.3cm]
    \textbf{React.js SPA (Thick Client)} \\
    \quad $\bullet$ RoutesPage.js (Route Planning) \\
    \quad $\bullet$ CameraMap.js (Camera Viewing) \\
    \quad $\bullet$ HistoryPage.js (User History) \\
    \quad $\bullet$ MapLayerControl.js (Map Layers) \\[0.3cm]
    \textbf{Visualization Engines:} \\
    \quad $\bullet$ Leaflet (Map Rendering) \\
    \quad $\bullet$ MapLibre GL (Vector Tiles) \\
    \quad $\bullet$ Material-UI (UI Components)
};

% Arrow and label
\draw[arrow] (presentation.south) -- ++(0,-2) node[midway, right, label] {\textit{RESTful API Calls}};

% Service Gateway Layer
\node[bigbox, below=2cm of presentation] (gateway) {
    \textbf{SERVICE GATEWAY (Interface)} \\[0.3cm]
    \textbf{src/api.js - Abstraction Layer} \\
    \quad $\bullet$ fetchCameraImages() \\
    \quad $\bullet$ searchLocation() \\
    \quad $\bullet$ calculateRoute() \\
    \quad $\bullet$ getSavedLocations() \\
    \quad $\bullet$ getSavedRoutes()
};

% Arrow and label
\draw[arrow] (gateway.south) -- ++(0,-2) node[midway, right, label] {\textit{HTTP Requests}};

% Application Layer
\node[bigbox, below=2cm of gateway, minimum height=5.5cm] (application) {
    \textbf{APPLICATION LAYER (Server - Backend)} \\[0.3cm]
    \textbf{Flask (Python) - API Server} \\
    \quad $\bullet$ Route Calculation (TomTom API) \\
    \quad $\bullet$ Location Search (TomTom API) \\
    \quad $\bullet$ Camera Image Proxy \\
    \quad $\bullet$ User Authentication (JWT) \\
    \quad $\bullet$ History Management \\[0.3cm]
    \textbf{External Services:} \\
    \quad $\bullet$ TomTom API (Routing \& Geocoding) \\
    \quad $\bullet$ OSM API (Map Tiles) \\
    \quad $\bullet$ HCMC Traffic Server (Camera Images) \\
    \quad $\bullet$ MySQL Database (Persistence)
};

\end{tikzpicture}
\caption{System Architecture: Three-tier architecture showing the Presentation Layer (React.js client), Service Gateway (API abstraction), and Application Layer (Flask backend with external services).}
\label{fig:architecture}
\end{figure}
